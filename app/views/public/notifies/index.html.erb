<p></p>

<div class ="container">
  <h3>通知一覧</h3>
  <div class="row">
    <% if @notifies.exists? %>
    <div class="col-md-8 my-5 mx-auto">
      <% @notifies.each do |notify| %>
      <table class="table table-borderless">
        <% if notify.action == "like" %>
        <tr>
          <td><%= link_to show_user_path(notify.notifier.id) do %><% if notify.notifier.image.attached? %><%= image_tag notify.notifier.image, size:'60 X 60' %><% else %><i class="far fa-smile"></i><% end %><% end %></td>
          <th><%= notify.notifier.club_name %>が　"<%= link_to recruit_path(notify.recruit.id) do %><%= notify.recruit.title %><% end %>"　に いいね しました</th>
        </tr>
        <% elsif notify.action == "message" %>
        <tr>
          <td><%= link_to show_user_path(notify.notifier.id) do %><% if notify.notifier.image.attached? %><%= image_tag notify.notifier.image, size:'60 X 60' %><% else %><i class="far fa-smile"></i><% end %><% end %></td>
          <th><%= notify.notifier.club_name %>から メッセージ が届きました</th>
        </tr>
        <% elsif notify.action == "entry" %>
        <tr>
          <td><%= link_to show_user_path(notify.notifier.id) do %><% if notify.notifier.image.attached? %><%= image_tag notify.notifier.image, size:'60 X 60' %><% else %><i class="far fa-smile"></i><% end %><% end %></td>
          <th><%= notify.notifier.club_name %>が　"<%= link_to recruit_path(notify.recruit.id) do %><%= notify.recruit.title %><% end %>"　に応募しました</th>
        </tr>
        <% elsif notify.action == "match" %>
        <tr>
          <td></td>
          <th>"<%= link_to recruit_path(notify.recruit.id) do %><%= notify.recruit.title %><% end %>"　で<% notify.notifier.club_name %>とマッチしました！</th>
        </tr>
        <% elsif notify.action == "cancel" %>
          <tr>
          <td></td>
          <th><%= notify.notifier.club_name %>との<% if notify.recruit.practice_type == true %>練習試合<% else %>合同練習<% end %>(<%= link_to recruit_path(notify.recruit.id) do %><%= notify.recruit.title %><% end %>)はキャンセルされました。</th>
        </tr>
        <% elsif notify.action == "match_rejected" %>
          <tr>
          <td></td>
          <th><%= notify.notifier.club_name %>との<% if notify.recruit.practice_type == true %>練習試合<% else %>合同練習<% end %>(<%= link_to recruit_path(notify.recruit.id) do %><%= notify.recruit.title %><% end %>)はマッチ不成立した。</th>
        </tr>
        <% else %>
        <tr>
          <td></td>
          <th>"<%= link_to recruit_path(notify.recruit.id) do %><%= notify.recruit.title %><% end %>"　の募集期限が過ぎたため、応募は削除されました。</th>
        </tr>
        <% end %>
      </table>
      <% end %>
    </div>
    <% else %>
    <div class="col-md-8 mt-5">
      <h5>新しい通知はありません</h5>
    </div>
    <% end %>
  </div>
</div>