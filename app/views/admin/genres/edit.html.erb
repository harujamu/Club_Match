<p></p>

<div class ="container">
  <h3>ジャンル名編集</h3>
  <div class = "row">
    <div class ="col-8">
      <%= form_with model: @genre do |f| %>
      <!--, url: admin_update_genres_path(@genre.id, method: :patch)-->
      <%= f.text_field :name %>
      <%= f.submit '更新する', class:"btn btn-primary" %>
      <% end %>
    </div>
  </div>
</div>